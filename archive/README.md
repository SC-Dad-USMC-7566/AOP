# archive/ — evidence locker (AOP)

Nothing in here is worked on. It exists so the migration can be audited.

- **drive-import/** — the "1. AOP Workshop" Google Drive tree exactly as exported on 2026-08-25, unzipped, untouched. Duplicate `(1)` files and retired material are preserved on purpose.
- **original-zips/** — the sealed .zip exported from Drive, kept so anyone can re-extract and confirm drive-import/ matches.

The working AOP tree is built by deliberate migration out of this locker. The locker never changes. AOP feeds the Ladder one way; the Ladder never feeds back into AOP.
