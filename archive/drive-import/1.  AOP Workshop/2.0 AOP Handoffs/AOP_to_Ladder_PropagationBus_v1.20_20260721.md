# AOP → Ladder propagation-bus note — canon movement v1.19 → v1.20

**Posted 21 July 2026.** The AOP basement moved with the v1.20 fold (four-axis deepening:
five corrections/re-grades + eight computed results, no claim retracted). The Ladder and
Time Machine threads inherit the following changes; each is flagged where it bears on a rung
or a route the Ladder maps onto.

1. **Drive → lifetime sign is now settled one-sided.** For a measure-preserving current
   (divergence-free, at fixed stationary distribution, small-noise), circulation can only
   shorten or leave unchanged the mean first-passage time — it never lengthens it
   [Lee & Seo 2021]. This is confirmed by computation (2D double well: μ₊(A)=1+√(9+8A²),
   escape accelerates, ratio ≤ 1). **Any Ladder rung that reads "drive lengthens
   persistence" must be re-scoped:** a star's fusion lengthens lifetime only because it is a
   *stationary-reshaping* drive the theorem does not bind (it acts through the forced Boundary
   and Memory edges and a present restoring force), not a counterexample to the one-sided rule.

2. **The hub gained two computed conditional edges.** B2 (screening residual I(in;out|F))
   and B4 (maintenance burden = housekeeping σ_hk = f·J) are now numbers, not assertions.
   Any Ladder use of "boundary maintenance costs drive" now has a worked, deposited basis.

3. **The framework's principal open problem is narrowed.** Level selection across a nested
   hierarchy is now synthesized (Φ^Max maximize-integration ordering, ported to AOP's
   static-Gaussian Φ_MIP; graded per Krakauer 2020), and the adiabatic *spatial* Φ_MIP is
   computed. The residual FRONTIER is only the **time-extended moving partition**. This bears
   directly on the §9 higher-individual route and the §9a collective-alive route the Ladder
   inherits: level selection is no longer a blanket open problem, only its time-extended form.

4. **Grade changes to carry:** sector-split σ̇-half upgraded FRONTIER→SETTLED-general;
   Drive→Integration (dynamic) re-graded to a cited tendency (not MaxEP/England as law).

**Status:** posted to the bus for Ladder/Time-Machine pickup. The AOP canon master is
`AOP_CANON_MASTER_v1.20.md` (1.0 AOP Canon); deposited Phase B–D scripts are in
`1.0 AOP Canon/Phase B-D scripts (reconstructed 20260721)`.
