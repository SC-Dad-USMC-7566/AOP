# AOP v1.21 — Apply Record (fold executed, awaiting placement)

**Prime, 21 July 2026.** The v1.20→v1.21 red-team corrections are folded into a rebuilt master,
verified, and delivered for manual placement (`AOP_CANON_MASTER_v1.21.md`, 208,518 bytes). **Not yet
on Drive as the canon master** — Ben places it; until then v1.20 remains live.

## Method
Downloaded the live master (`1EB8F4L6...`, 201,962 B, Canon folder — the clean copy, not the staging
copy), decoded, edited programmatically (anchored single-replacement, every OLD span sliced from the
live bytes, each edit asserted exactly one match), diffed against the original.

## Edits applied (8)
1. **D1** §12 Current→lifetime cell — star de-scoped (out-of-scope ≠ positive mechanism; names the
   required intervention + exit set + paired counterfactual). Body line 735.
2. **D1b** §13a — added "Stellar positive-persistence mechanism [FRONTIER, parked]" open item.
3. **D2** §4 Drive→Integration edge — re-graded to a model-scoped feasibility/necessary-condition
   constraint; no D→I tendency, no causal arrow (v4 text, all four of Aster's scope points folded).
   Body line 137.
4. **Table 2 D→I row** — tightened from "free (static)/open (dynamic) · known/open" to the
   feasibility-constraint wording, matching D2. (Note: rev2's assumed OLD tag "cited tendency" was
   wrong — the live table already read "open"; verification caught this. The body, not the table, was
   the overstated side.)
5. **Masthead** (line 13) — version stamp 1.20→1.21; v1.21 summary clause prepended; v1.20 lineage
   left intact (append-only).
6. **Changelog** — v1.21 entry appended; v1.20 entry preserved **byte-identical** (confirmed).
7. **Reference list** (line ~946) — 2410.13375 entry updated from "[Authors TBD] … arXiv (2024)" to
   **Ptaszyński K, Esposito M, Phys. Rev. Lett. 135, 057401 (2025)**. *Caught in verification: the
   inline cites were updated but the bibliography entry was not — an internal contradiction that would
   have shipped.*

## Verification
Final diff touches only 6 regions (lines 13, 137, 735, 778-insert, 946, changelog); everything else
byte-identical. Headings 24/24, tables 15/15. "conditional tendency" gone from body; surviving
"cited tendency"/"reconciled" strings are all in v1.20 history (append-only). "Authors TBD" reduced to
one deliberate quote in the v1.21 changelog. Citation independently line-checked against the primary
(Ptaszyński & Esposito; PRL 135, 057401 (2025)) — v1.20's ⚠ discharged.

## Still open (not part of the canon fold)
- **D3 — planning-doc only.** The "1 of 15 genuine new work" score correction applies to the gap plan
  and `AOP_FourAxis_Combined_Report.md`, NOT the canon (confirmed: no such tally in the master). To do.
- **Propagation-bus note.** The Drive→Integration re-grade + star de-scope touch the hub inventory →
  post an AOP→Ladder bus note.
- **Full-text proposition-number check** on 2410.13375 before journal submission (authors/venue/scope
  are confirmed; only the internal Lemma/Thm numbers remain).

## Placement instruction
Place `AOP_CANON_MASTER_v1.21.md` into the Canon folder (`1V_ufLQWTXVrUmVVGVth2ExFqXrBahw_J`).
Supersedes the v1.20 master. Do not fold from the dev-tree staging copy.
