# Handoff to Aster — Moving-MIP v4, after your second pass (v3 of this note)

**From:** Claude Science (builder lane), AOP · **Date:** 21 July 2026
**Location:** Google Drive → **Canon Development** (`1Yta-E7kdOS69IboXR3_CT5-aLcxB8XA4`)

Hi Aster — thank you for the second pass. You were right that the v3 set wasn't internally clean, and every point reproduced. I've folded all six Priority-1 items plus the Priority-2 grading split into a **v4** deposit. Here's what changed and where it is.

## What I verified and fixed

**The λ=0 mismatch — confirmed, and it proves your point.** The deposited v3 script prints `rot=18, events=9` on this interpreter; you saw `12/6`. Both are arbitrary: with the penalty off, per-slice ties (the degenerate singleton MIPs) make the path a tie-breaking artifact of enumeration order. v4 marks the λ=0 rot/events **non-identifiable** and reports the meaningful limit instead — **λ→0⁺ with a minimum-rotation tie-break: rot 2, one change event**. Only J/T and the deficit part are quoted at λ=0.

**The other five, all fixed:**
- **§3.2 heading** "spectral surrogate … convex relaxation" → "Independent spectral diagnostic (inflated supra-Laplacian analogy — not a surrogate or relaxation)."
- **§3.1** "annealed-soft-partition reading" → "hard change-point / partition-path regularization," attributed to the builder (the annealing refs motivate a *future* soft version; the nearer template is Chen–Kawadia–Urgaonkar, which you independently confirmed).
- **Panel B** retitled "value function is continuous **in λ**," with an explicit sentence that continuity through developmental time / a sliding window is **not** tested. This is the conceptual correction I most needed — thank you for it.
- **§1 excess-entropy** overstatement replaced in place with the scoped formulation (past–future MI at a fixed cut remains definable; the stationary invariant/asymptotic form is what fails), so §1 no longer contradicts §5.
- **Reproducibility gap closed**: v4 script has a `make_figure()` function and a `--figure` flag; the deposited figure is now generated by the deposited code. (The v3 script genuinely had no plotting code — correct catch.)

**Priority-2 also done:** the merged `a`/`λ` grading row is split — the discrete endpoint limits graded as direct properties of the builder's objective; the supra-Laplacian `a→∞` limit graded as an *analogous* graph result (not FK's theorem reproduced), with the note that the `a≤10` sweep shows monotone movement, not convergence (1.03 vs 4.80).

**And the numerical detail:** at the exact crossing `b*=0.4206007` the minimizer set is the module cut **plus** six 1|5 singleton cuts = **seven** tied minimizers; the text now says seven, not six.

## Verdicts I've adopted (unchanged from your review)

- **Research direction: YELLOW** — the hard partition-path objective is a legitimate, exactly-solvable candidate worth repairing.
- **Canon closure: RED** — §4, §9a, §13a stay FRONTIER. Canon never edited.
- The v4 deposit is now, to the best of my checking, internally consistent — but per "nobody grades their own homework," that's your call and Prime's, not mine to certify.

The three Priority-2/creative research directions (fix time-discretization before the soft extension; sliding-window / ramp-speed / sampling-density tests; a symmetry-broken two-competing-partition benchmark with all tied paths exposed; and your "treat degeneracy as data — track the set-valued argmin" idea) are captured in §8 as the repair roadmap. I have **not** started them — awaiting Ben/Prime direction on whether to attempt the repair-and-retest.

## Current files (use the `v4` stamp)

| File | Drive id |
|---|---|
| `AOP_MovingMIP_Build_proposal_v4_20260721.md` | `1wxSAE10FxoG9McEYRBALX2u2_cp14z0f` |
| `phaseE_movingMIP_v4_20260721.py` | `1_dN14YTylD5LfM5nGVJ7_DNHPYj9xWHK` |
| `phaseE_movingMIP_fig_v4_20260721.png` | `1sOpGXj-YdNk6avPg5Gsc4RftQT7gaJ_K` |
| `AOP_LineCheck_PtaszynskiEsposito_20260721.md` (unchanged) | `13v_CB79dF6y-930Bg8esCKTOB9C-ym3_` |

The connector can't overwrite/delete, so v1–v3 generations still sit in the folder; **ignore anything without `v4`** in the name. Ben is clearing the older ones.

Thank you again — two passes materially improved this.

— Claude Science (builder). Prime verifies; Ben decides.