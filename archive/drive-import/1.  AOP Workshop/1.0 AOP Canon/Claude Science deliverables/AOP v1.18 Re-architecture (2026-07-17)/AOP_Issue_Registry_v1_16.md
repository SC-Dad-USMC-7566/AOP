# AOP Issue Registry — OAI Remediation, mapped to frozen v1.16

**Purpose.** OAI Master Plan, Phase 0, action 4: *"Create an issue registry that maps each criticism to its source location, severity, proposed disposition, evidence requirement, and verification test."* This is the master control document for the maximal re-architecture. Every row is pinned to the **frozen baseline** `FROZEN_aop_canon_v1_16.md` (MD5 `241153bc…`), by section + anchor phrase (stable under the Drive export's markdown escaping), not line number.

**Compiled:** 17 July 2026, this session. **Status:** governance artifact; records dispositions to execute, not canon. Every disposition marked **[verified vs text]** was checked by direct grep of the frozen file this session; those marked **[to verify in rebuild]** are asserted by OAI and accepted pending the rebuild pass.

**Severity key:** P0 = stop-ship · P1 = publication-strength · P2 = research-program (post-submission or explicitly-scoped future work).
**Dependency-status / evidential-status** columns use the two-axis labeling adopted in Phase 1 (see the ADR).

---

## P0 — stop-ship

| ID | OAI item | v1.16 location (anchor) | Disposition (vs actual text) | Evidence requirement | Verification test |
|---|---|---|---|---|---|
| **P0-1** | Binding manufactures a rest frame / clock / proper time / present tense | §10, "Binding manufactures a rest frame, and with it a clock…"; also "What binding universally supplies is the rest frame and the present tense" | **[verified vs text] Real, partial defect.** The relativity part (any bound, localized, timelike system has a rest frame + proper time) is secure physics. The AOP part (a *present tense* to defend) is synthesis. The verb "manufactures" + lumping the two blurs a graded synthesis into a pseudo-derivation. Already graded honestly at §10 close, so "remove it" (OAI) overshoots. | Split the secure relativity claim from the AOP synthesis claim; cite the relativity part to a standard source; keep the present-tense part graded [SYNTHESIS]. | Rebuilt §10 has two separately-graded sentences; a hostile reader cannot read the present-tense claim as a theorem. Soften "manufactures"→"supplies/entails." |
| **P0-2 (Memory)** | E = excess entropy mislabeled "stored memory" | §4 / status table, "the quantity that compels (E) and the quantity that does the persisting work (statistical complexity Cμ) can come apart [13]" | **[verified vs text] Already done.** E and Cμ are explicitly separated; canon never equates E with stored memory. "irreducible integration" = 0 hits. | None (confirm no regression in rebuild). | grep rebuilt draft: E is called "predictive dependence"/"excess entropy," never "stored memory"; Cμ retained as the storage measure. |
| **P0-2 (Integration)** | TC mislabeled "irreducible integration" | §4, "total correlation … cannot separate… which is why it, and not total correlation, reads two independent modules as…"; Φ_MIP introduced separately | **[verified vs text] Already done.** TC labeled the redundancy flavor, distinct from Φ_MIP (irreducibility). "irreducible integration" absent. | None (confirm no regression). | grep rebuilt draft: numeric Integration claims name the flavor (I1 TC / I2 O-info / I3 Φ_MIP); no bare "Integration = irreducible." |
| **P0-2 (Drive)** | σ glossed as "free-energy throughput" (it is the *dissipated* part) | §1 one-paragraph claim, "a free-energy throughput that holds the system off equilibrium (Drive)"; status table "entropy production rate σ (trajectory time-asymmetry)" | **[verified vs text] Real, thin. Fix required.** σ is the dissipated/irreversible component, not total throughput. Intro prose conflates them. | Relabel: Drive = conceptual target; σ = "dissipation/irreversibility" (D5 in the panel). Add one sentence that σ ≠ resource input / useful work. | Rebuilt Drive text uses "dissipation/irreversibility" for the σ axis; a reader cannot infer high resource input from σ alone. |
| **P0-2 (Boundary)** | Raw I(inside;outside) presented as boundary strength | status table "Boundary (B) | space | mutual information I(inside ; outside)"; §1 "Each is a computed difference across a cut—a mutual information" | **[verified vs text] Real. Fix required.** MI across a cut measures dependence, not separation/screening/maintenance. High MI can mean mediation, leakage, or common input. | Decompose Boundary into the B1–B5 panel (state-contrast / interface-mediation / leakage / maintenance-burden / cross-boundary-dependence). Retain MI, labeled exactly as dependence. | Rebuilt Boundary text: "strongly bounded" is asserted only after naming which of B1–B5 is high/low; MI never stands alone as boundary strength. |
| **P0-3** | "Drive and Memory are choice-free" language | **Absent** — grep "choice-free" / "choice free" = **0 hits in frozen v1.16** | **[verified vs text] Stale / N/A.** Targets pre-v1.16 text (OAI's source set includes "the v1.5 manuscript"). Do not act on v1.16. | None. Records that the P0 list must be re-pinned to v1.16 (done here). | Confirm 0 hits (done). If the OAI-proposed replacement wording ("no additional spatial/component cut after a process representation, time grain, reversal convention are fixed") is wanted, it is an *addition*, not a correction. |
| **P0-4** | "the system's *own* viability function" (self-supplied) | Abstract subtitle "a semantic mask read out by a system's own viability"; §7 "relative to the persisting system's own viability function"; 15 hits | **[verified vs text] Real, but narrower than OAI states.** The defect is specifically "own = self-supplied" (singular, self-owned). The method already uses a declared family of functionals. **Critical nuance:** the canon's "No ownership" refusal and "ownership-free" individuation axis are the *correct* PIC-lesson framing the charter wants **preserved** — do NOT delete the word "ownership." | Change "own/its own viability function" → "an analyst-declared, dynamically-constrained family of viability functionals." Retain "own" only where it means "indexed to the system's viability set." Keep the "No ownership" refusal verbatim. | grep rebuilt draft: no instance of "own viability function" meaning self-supplied; the "No ownership" refusal is unchanged; §3/§7/§11a reference the declared family. |
| **P0-5** | Evidential status fused with dependency status in the status table | §12, Table 3 "Status" column; warrant type buried in prose caveat column | **[verified vs text] Real, cheap, structural.** One column fuses evidential grade (secure/synthesis/frontier) with dependency scope (scoped/static-Gaussian/frontier-at-generalization). A hostile reader can read "secure, scoped" as overstating a one-example result. | Split into two independent columns: **dependency status** (forced / conditionally-forced / dissociable / unidentified) × **evidential status** (theorem / definition / counterexample / analytic / numerical / ensemble / empirical / conjecture). | Rebuilt Table 3 has two structured columns; each claim's warrant type is explicit; no single "Status" cell carries both meanings. |
| **P0-6** | Reproducibility / version reconciliation | Package-level: 3 non-identical "v1.16" Drive files; README/manifest count disagreement | **[verified vs text] Real, mechanical — RESOLVED (see Version & Count Reconciliation).** Frozen baseline designated (`(2)`, MD5 `241153bc…`). Reference-list = **52 entries** (authoritative, per punchlist); canon cites **~26 distinct works inline** in a mixed style (13 numeric `[n]` + 13 author-year, whitespace-normalized). The README/manifest count divergence ("25 vs 31 verified-in-body; 8 vs 2 needs-user-PDF") is a stale-snapshot artifact: +6 Tier-1 PDFs reconcile the delta exactly (25→31, 8→2). README numbers are stale; punchlist is authoritative. | Regenerate README + manifest from the punchlist; reconcile Data-Accessibility to the 52-entry / ~26-inline structure; delete stale "Drive master v1.14" line; adopt one citation style in rebuild. Acquire 2 print-only books (**needs Ben**). Archive the 2 superseded v1.16 copies. | One frozen file; README/manifest/Data-Accessibility all match punchlist (31/14/5/2 of 52); superseded copies archived. |

## P1 — publication-strength (the re-architecture proper)

| ID | OAI item | v1.16 location | Disposition | Evidence requirement | Verification test |
|---|---|---|---|---|---|
| **P1-1** | Operational panels: Boundary/Drive/Memory/Integration each a declared proxy *family*, not one scalar | status table (four single-scalar rows); §1–§7 | **Adopt (ADR Phase 1).** This is the architecture change the maximal path commits to. Each target → B1–B5 / D1–D5 / M1–M5 / I1–I5. | Full panel spec (ADR); at least one worked example computing the panels (the benchmark, Phase 3). | Every axis label in the rebuilt tables/figures names the exact proxy flavor; the benchmark reports the full panel. |
| **P1-2** | Common declaration tuple D = (S,E,F,P,δt,τ,R,V,I,N) on every reported profile | absent as an explicit requirement | **Adopt (ADR).** No reported value is an invariant without D. | ADR defines D; benchmark + archetypes each carry a D. | Every computed result in the rebuilt draft is accompanied by its declaration tuple. |
| **P1-3** | Finite-horizon viability family V(θ,τ), not a single owned scalar | §3, §7, §11a (mask read out vs "own viability") | **Adopt (Phase 2/3).** V(θ,τ) = prob. of remaining in / returning to viability set over τ, across a declared family (config/process/recovery/capacity). | Formal definition + the family used in the benchmark. | Benchmark reports load-bearing-ness across the family, not one convenient functional. |
| **P1-4** | Admissible interventions: declare what each intervention changes and holds fixed | §3 semantic mask ("scramble-and-re-run"), §11 | **Adopt (Phase 2).** Intervention classes I-1…I-6, required invariants list, physical-status labels (realizable / interpretable / diagnostic / inadmissible). | Preregistration doc lists each intervention operator + invariants. | A third party can state exactly what each benchmark intervention changes and fixes. |
| **P1-5** | Coalition-aware semantics; per-edge weights only when isolable/additive; else semantic hypergraph | §3 Figure MW (single-edge mask on driven ring) | **Adopt (Phase 2).** ΔV(S) over coalitions; minimal failure cut-sets, viable sets, redundant alternatives, synergistic coalitions, Möbius h(S). | The benchmark computes the full coalition table closed-form + the hypergraph. | Benchmark detects the redundant-bypass and synergistic-pair controls without misreporting them as independent edges (Gate D). |
| **P1-6** | One complete benchmark: a system whose answer is NOT built in | **absent** — the OAI benchmark spec doc is an empty stub | **Build (Phase 2 — keystone).** Minimal leaky autocatalytic compartment as a closed-form CTMC. | The model, preregistration, results, negative controls, figures. | **Exit gate:** ≥1 preregistered result not guaranteed by construction, could have failed, and changes the system's classification. |
| **P1-7** | Narrow the Drive→Memory implication to predictive dependence only | §1 "dissipation forces a floor on predictive memory (excess entropy, a corollary of trajectory irreversibility) but reaches no further" | **[verified vs text] Already largely done.** Canon already bounds the D→M floor to predictive memory and says "reaches no further." | Confirm wording; ensure floor is on M1 only, not M2–M5. | Rebuilt draft: the σ>0 ⇒ E>0 corollary is stated as a floor on predictive dependence (M1) and explicitly not on stored/depth/semantic memory. |
| **P1-8** | Stop treating shared KL form as proof of commensurability | §"No master quantity", "The common form is what makes them commensurable" vs "thinner than it first appears" | **[verified vs text] Mixed — real residual defect.** Canon already refuses a master quantity and hedges ("thinner than it first appears"), but the word "commensurable" still licenses the prohibited inference (comparing bar heights / a master magnitude). | Reword: relative entropy = common *vocabulary*, not commensurability. Rates (σ) vs amounts (B,M,I) need a common horizon (Σ_τ = τσ) or separate panels before any graphical comparison. | Rebuilt draft: no claim that shared KL form makes values comparable; any 4-axis fingerprint figure is labeled qualitative unless a justified normalization is stated. |
| **P1-9** | "Energy is the hub" as organizing hypothesis, not structural fact; reliability is a consequence, not a 5th axis | §"Energy is the hub, but its spokes are uneven… the thermodynamic uncertainty relation prices reliability" | **[verified vs text] Real, cheap.** Hub language present; reliability already framed as priced consequence (good), but "hub" reads as established structure. | Reframe hub as [SYNTHESIS]/organizing hypothesis unless the graph among the four declared targets actually supports hub status. | Rebuilt draft: "energy is the hub" carries an explicit synthesis grade or is stated conditionally on the target-graph; reliability not listed as an AOP axis. |

## P2 — research-program (post-submission or explicitly-scoped)

| ID | OAI item | v1.16 location | Disposition | Verification test |
|---|---|---|---|---|
| **P2-1** | Comparative adjudication vs one named one-axis rival | absent | **Do (Phase 5).** Score the benchmark under a named comparator without weakening it; predefine information-gain/misclassification. | AOP makes ≥1 measurable distinction the rival does not — or the paper accepts it is an organizing vocabulary. |
| **P2-2** | Quarantine gravity / screenability / light-cone from the scientific core | §8 "What can make a boundary: screenability"; star case; §"screenable (electromagnetism)…" | **Scope out (Phase 1/4).** Move to a separate taxonomy-of-boundary-notions appendix or the Ladder; not in the core claims. Consistent with the prior genre decision (photon/horizon set-pieces already parked to the Ladder). | Core claims of rebuilt draft do not depend on the gravity/screenability argument; it appears (if at all) as explicitly-scoped separate material. |
| **P2-3** | Relabel crystal/flame/spore/atom/star as *diagnostic archetypes* until all quantities computed | five worked cases across §§ | **Do (Phase 3).** Relabel; mark which declarations/quantities are computed vs illustrative. Reconcile MFPT/mask numbers against the **canonical absorbing-window values**, not the unverified prior baseline set. | Rebuilt cases labeled "diagnostic archetype"; each quantity marked computed/derived/schematic/illustrative. |
| **P2-4** | Nested individuation, living threshold, collective agency, cosmology, Ladder propagation | §9, §9a, §11a, scattered | **Scope out (Phase 6 outlook).** Present as explicitly downstream work; stop adding until the operational layer is stable (Phase 0 action 5). | Outlook section frames these as downstream; no new such material added during the rebuild. |

## Items needing Ben

1. **Two print-only books** — Ashby, *Design for a Brain* (1960); Parfit, *Reasons and Persons* (1984). Cannot be verified against a retrievable primary source this session. Any [S]-graded claim leaning on them is blocked until acquired.
2. **Archive the 2 superseded v1.16 Drive copies** (`aop_canon_v1_16.md`, `aop_canon_v1_16 (1).md`) so no future session picks the internally-inconsistent base copy.
3. **Venue confirmation** — the rebuild is written to a Perspective/atlas posture (already the title-block genre + the prior genre decision on record). If that has changed, the panel architecture's scope commitments change with it.

## Tally

- **Already done in v1.16 (confirm-no-regression):** P0-2 Memory, P0-2 Integration, P1-7 (D→M floor).
- **Stale / not in v1.16:** P0-3 (choice-free).
- **Real cheap prose/table fixes:** P0-1, P0-2 Drive, P0-2 Boundary, P0-4, P0-5, P1-8, P1-9.
- **The re-architecture (adopt):** P1-1…P1-6.
- **The keystone (build, currently absent):** P1-6 benchmark.
- **Scope-out / future:** P2-1…P2-4.
